# KunoWorld generated ocean media

Generated through OpenRouter using the authorized local key. No secret is stored in this folder or shipped to the browser.

- Images: `openai/gpt-image-2`, high quality, original resolution 1536 × 864.
- Videos: `google/veo-3.1`, 1920 × 1080, 24 fps, eight seconds each, no audio.
- Actual batch charge: **$5.450315** out of the user-authorized **$50** budget. No outstanding jobs or paid retries.

## Website assets

`public/media/hero-cove.webp`, `horizon.webp`, `shoreline.webp`, `sailboat.webp`, and `limestone.webp` are optimized WebP stills. The three matching scene names also have `.mp4` videos, encoded with H.264 CRF 27, a 3.5 Mbps rate ceiling, and fast-start metadata. Each video has a `-poster.webp` extracted from its first frame to avoid a visual jump from an independently generated still.

Original generation files remain at `/tmp/kunoworld-ocean-assets` for this session; the permanent website derivatives are in `public/media`. All three videos passed full decode checks, and the images and sampled video frames were visually inspected.

See `asset-ledger.json` for prompts, settings, job IDs, and exact reported charges. Its `path` values document original generation output; `website_path` identifies the preserved derivative. The internal media allocation was capped at $35, below the user's overall $50 authorization.

These assets illustrate the website's creative direction. They were produced with external OpenRouter models and do not represent current KunoWorld GPU inference or confidentiality guarantees.

## Showcase expansion

Four additional stills and four eight-second 1080p films cover neon cinema, product design, a speculative world, and vertical fashion. Their briefs and exact charges are recorded in `showcase-briefs.json` and `showcase-asset-ledger.json`. The expansion cost **$6.919835**, bringing the complete media collection to **$12.370150** of the authorized $50 budget. All jobs finished without paid retries. Matching first-frame posters and optimized derivatives are preserved in `public/media`; all seven films use the encoding settings above.
