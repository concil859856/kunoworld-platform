import type { Metadata } from "next";
import { ArrowUpRight } from "lucide-react";
import { CodeBlock, SDKExample } from "@/components/site/code-block";
export const metadata:Metadata={title:"Documentation — KunoWorld",description:"Get started with KunoWorld Studio and SDKs. Learn generation modes, model routing, encryption, receipts, and current development status."};
const sections=[['introduction','Introduction'],['studio','Using the studio'],['privacy-modes','Private or Standard'],['keys','Keys & recovery'],['share-links','Share links'],['quickstart','SDK quickstart'],['creation-modes','Creation modes'],['model-routing','Model routing'],['receipts','Results & receipts'],['errors','Troubleshooting'],['project-status','Project status']];
export default function Docs(){return <div className="docs-layout"><aside className="docs-sidebar"><span>GET TO KNOW KUNOWORLD</span><nav aria-label="Documentation sections">{sections.map(([id,label])=><a href={`#${id}`} key={id}>{label}</a>)}</nav><a href="/api" className="docs-external">Full API reference <ArrowUpRight size={13}/></a></aside><article className="docs-main"><span className="section-kicker">DOCUMENTATION / GETTING STARTED</span><h1 id="introduction">Your first world<br/><em>starts here.</em></h1><p className="docs-lead">Create with the studio or build with the SDK. Both use the same KunoWorld generation protocol.</p><div className="notice"><strong>Before you begin.</strong> This project is a development preview. To use the studio, sign in with your email; you don’t need an API key. To build with the SDKs, you need a running KunoWorld gateway, an API key, and a trusted golden manifest. Mock workers return placeholder video. The showcase films on the website are externally generated creative assets.</div><section id="studio" className="docs-section"><h2>Create in the studio</h2><ol><li><a href="/signin">Sign in</a> with your email. We send you a one-time link; there is no password and no API key to enter.</li><li>Open <a href="/studio">the studio</a>. Choose how to start — text, frames, keyframes, references, or an edit of an existing clip. Describe your scene and select the model, duration, resolution, and aspect ratio.</li><li>Choose who can see the take: <strong>Private</strong> (the default) or <strong>Standard</strong>. The studio remembers your choice in this browser.</li><li>Select <strong>Generate video</strong>. For a private take the studio checks the worker’s evidence and encrypts your prompt and inputs in this browser before they leave it; a standard take is uploaded to KunoWorld as it is.</li><li>Find the video and its signed receipt in <strong>My creations</strong>.</li><li>When you want a video gone, select <strong>Delete</strong> in My creations. The stored video is removed; billing records, job metadata and the signed receipt stay.</li></ol><p>The studio talks to KunoWorld through this website’s own server. Your session is kept in an HttpOnly cookie that page scripts can’t read, so the browser never holds an API key or gateway token. For a private take, the website’s server only passes the encrypted data through.</p><p>The keys that open your private takes stay in this browser, unless you turn on <a href="#keys">key sync</a>, which also keeps them with your account, encrypted so that KunoWorld can’t read them. The studio saves each private take in this browser’s local storage — its prompt, its settings, and the job handle that holds its output decryption key — so your library survives a reload. Decrypted videos are not saved: a take is downloaded and decrypted again when you open it. <strong>Back up your keys</strong> with <strong>Back up film keys</strong>: if a key is lost, the video is lost, and KunoWorld can’t recover it. <strong>Forget all</strong> removes the keys from this browser. Anyone who can use this browser profile can open those films. Standard takes are listed from your KunoWorld account instead.</p><p>Only you can open your videos, unless you create a <a href="#share-links">share link</a> for one.</p></section><section id="privacy-modes" className="docs-section"><h2>Private or Standard</h2><p>Every take has a privacy mode, chosen in the studio or with the SDKs’ <code>privacy</code> option.</p><div className="docs-card-grid"><div className="docs-card"><h3>Private (default)</h3><p>Encrypted in your browser or program to an attested confidential GPU. The stored video opens only with its key, which stays on your devices; <a href="#keys">key sync</a> stores it only encrypted, and a <a href="#share-links">share link</a> carries it only after the <code>#</code>. KunoWorld sees metadata only: account, model, duration, resolution, aspect ratio, frame rate, input roles, sizes, timing, price, status, and the receipt.</p></div><div className="docs-card"><h3>Standard</h3><p>KunoWorld’s systems and the GPU provider, which may not use a confidential GPU, can read your prompt, inputs, and video. In return it can run on any GPU. In the product, only your account can open it, unless you create a <a href="#share-links">share link</a> for it.</p></div></div><p>In both modes, videos are stored on Cloudflare R2 until you delete them. Nothing expires automatically. Content under a legal hold or a child sexual abuse material report is kept until the hold ends.</p><p>KunoWorld moderators and administrators can open content only for a report of child sexual abuse material or sexual content involving a minor, or under a legal hold, and every view is logged. There is no sampled review. To open a private video they also need its output key, supplied in such a report.</p><p>All sexual or explicit content is banned in both modes. A standard request that breaks the content policy is refused with <code>422 content_policy</code>; a private take blocked inside the confidential GPU fails with <code>safety_blocked</code>. Blocked takes count as strikes, and too many pause the account for a while. Private mode also needs an account in good standing: by default a credited top-up, no active restriction and fewer than 2 blocked takes in 30 days. Your <a href="/account#private-mode">account page</a> shows whether it’s available. Anyone can <a href="/report">report a video</a>.</p><CodeBlock code={'// JavaScript\nconst job = await kuno.submit({ prompt, model: "ltx-2.5-fast", privacy: "standard" });\nconst { video } = await kuno.wait(job);\n\n# Python\nresult = kuno.generate(prompt, model="ltx-2.5-fast", privacy="standard")'}/><p>Handle these errors: <code>private_mode_not_eligible</code> (with <code>reasons</code>), <code>account_restricted</code> (with <code>restricted_until</code>), <code>content_policy</code>, <code>safety_blocked</code> and <code>upload_blocked</code>. See the <a href="/api#privacy-modes">API reference</a>.</p></section><section id="keys" className="docs-section"><h2>Keys &amp; recovery</h2><p>Each Private video has an output key that only you hold. Without key sync, keys live only in the browser that made the video and in any backup files you download. <strong>Key sync</strong> is optional: it keeps your keys with your KunoWorld account, encrypted in your browser, so you can open your Private videos on any device you sign in to. Set it up and manage it under <strong>Keys &amp; recovery</strong> on your <a href="/account">account page</a>.</p><h3>How your keys are protected</h3><ul><li>When you turn key sync on, your browser makes an account master key: 32 random bytes that never leave your devices unencrypted.</li><li>For each Private video, your browser encrypts its key record with AES-256-GCM under the master key, bound to your account and that video. The record holds the output key, the enclave’s signing public key, the content digest, and the take’s display details, such as the first 500 characters of the prompt and its settings. KunoWorld stores only the ciphertext.</li><li>The master key is itself encrypted (“wrapped”) by one or more unlockers: your recovery code and, if you add one, a passkey.</li></ul><div className="docs-card-grid"><div className="docs-card"><h3>Recovery code</h3><p>Shown once, when you set up key sync: 32 characters (160 random bits), grouped in fours. Your browser stretches it with PBKDF2-HMAC-SHA256 at 600,000 iterations and a random salt, the current recommendation of the <a href="https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html">OWASP Password Storage Cheat Sheet</a>. Write it down and keep it somewhere safe: KunoWorld can’t recover a lost recovery code.</p></div><div className="docs-card"><h3>Passkey (optional)</h3><p>Unlocks your keys with the WebAuthn PRF extension, where your browser and authenticator support it. Not all do: support differs, for example, across browsers and for security keys on iOS. The passkey doesn’t sign you in; it only unlocks your keys on this site.</p></div></div><h3>On a new device</h3><ol><li><a href="/signin">Sign in</a> with your email.</li><li>Unlock your keys with your recovery code or a passkey.</li><li>Open <strong>My creations</strong>. Your Private takes are there, with working keys.</li></ol><p>Keys already saved in a browser upload, still encrypted, once you unlock key sync in that browser.</p><h3>What KunoWorld holds</h3><p>For each unlocker: the wrapped master key, its public parameters (for a recovery code, the salt and iteration count; for a passkey, the credential ID, PRF salt and site ID) and its label. Also the wrapped key records, which videos have a synced key, and when things changed. KunoWorld can’t decrypt any of it.</p><h3>Managing key sync</h3><ul><li><strong>Back up film keys</strong> and <strong>Restore keys</strong> still work, with or without key sync.</li><li>Deleting a video also deletes its synced key.</li><li><strong>Turning key sync off</strong> deletes your vault from KunoWorld. Keys already in a browser stay there.</li><li><strong>Rotating keys</strong> makes a new master key and a new recovery code. Old recovery codes and passkeys stop working; you can add passkeys again afterwards.</li><li>Key sync works only through the website’s email sign-in, never with an API key.</li></ul><div className="notice"><strong>A lost key is still a lost video.</strong> If you lose every device, your recovery code and your passkeys, KunoWorld can’t recover your keys, and your Private videos can’t be opened.</div></section><section id="share-links" className="docs-section"><h2>Share links</h2><p>Only you can open your videos, unless you create a share link for one. Links are off by default.</p><ol><li>In <strong>My creations</strong>, select a finished video and create a link from its inspector.</li><li>Choose when the link expires: never, or at a time you pick.</li><li>Send the link to the people you want to watch. Anyone who has it can watch that video.</li></ol><p>A link is shown only when you create it. KunoWorld stores only a hash of its token (32 random bytes), so a lost link can’t be shown again: make a new one. Your <a href="/account">account page</a> lists your links and lets you revoke them. A video can have up to 20 working links.</p><div className="docs-card-grid"><div className="docs-card"><h3>Standard video</h3><p>KunoWorld serves the video to whoever has the link.</p></div><div className="docs-card"><h3>Private video</h3><p>The link carries the video’s key after the <code>#</code>, as in <code>{'https://kunoworld.com/s/<token>#k=<key>'}</code>. Browsers never send that part to a server, so KunoWorld still can’t see the video: the share page downloads the encrypted video, decrypts it in the viewer’s browser and checks its enclave-signed receipt. Anyone with the full link can watch, so share it as carefully as the video itself. A link missing its key shows a message saying so.</p></div></div><h3>When a link stops working</h3><p>A link stops working when you revoke it, when it expires, when you delete the video, when the video is removed after a review under the content policy or placed under a legal preservation hold, or when the account is closed. Viewers see the same “This link no longer works” message in every case.</p><h3>Views and viewers</h3><p>Share pages are marked noindex and aren’t cached. KunoWorld counts views, but doesn’t record who viewed. IP addresses aren’t stored: public share requests are rate-limited per IP using a keyed hash, 60 requests a minute by default.</p><h3>The rules still apply</h3><p>Shared videos follow the same content policy. An account under a restriction can’t create links, and anyone who sees a shared video can <a href="/report">report it</a>. Share links don’t change when KunoWorld operators can open content: only for a report of child sexual abuse material or sexual content involving a minor, or under a legal hold.</p><p>Developers can manage links with an API key; see <a href="/api#share-links">share links in the API reference</a>. The JavaScript and Python clients provide <code>shares.create</code>, <code>shares.list</code>, <code>shares.revoke</code>, <code>shares.get</code> and <code>shares.open</code>.</p></section><section id="quickstart" className="docs-section"><h2>Make your first SDK request</h2><p>This section is for developers calling KunoWorld from their own programs. The SDKs authenticate with an API key, which you create on your <a href="/account">account page</a>. Keep API keys on your server: never ship one to a browser. The project includes JavaScript and Python clients. Package publication is still pending; use the SDK source from your KunoWorld workspace. The JavaScript client runs in Node 20.19+ and modern browsers. The examples below run in Node.js or Python.</p><h3>Terminal 1: start the local network</h3><p>From the KunoWorld workspace root, initialize the development environment and start the gateway and mock worker. Keep this terminal running while you use the SDK; stopping it shuts down both services.</p><CodeBlock code={'uv sync\nscripts/dev.sh'}/><h3>Terminal 2: prepare the examples</h3><p>Open another terminal at the same workspace root. These commands use the default <code>data</code> directory and gateway port. The development tool creates <code>data/dev.env</code> and <code>data/manifest.json</code>. Load its environment, map <code>KUNO_DEV_API_KEY</code> to the examples’ <code>KUNO_API_KEY</code>, and copy your locally generated manifest beside the example files.</p><CodeBlock code={'. ./data/dev.env\nexport KUNO_GATEWAY_URL\nexport KUNO_API_KEY="$KUNO_DEV_API_KEY"\ncp "$KUNO_MANIFEST" ./trusted-manifest.json\n\n# Build and install the JavaScript SDK for this workspace.\nnpm --prefix sdk/js install\nnpm --prefix sdk/js run build\nnpm install ./sdk/js'}/><p>If you configured another data directory or port, use that environment file and set <code>KUNO_GATEWAY_URL</code> to the URL printed in terminal 1. For a remote gateway, use an API key from your account page and obtain the manifest from your operator through a trusted channel. Never put an API key in frontend build variables.</p><SDKExample/><p>Save the JavaScript example as <code>example.mjs</code> at the workspace root and run <code>node example.mjs</code> in terminal 2. For Python, save <code>example.py</code> there and run <code>uv run python example.py</code>; <code>uv sync</code> already installs the Python SDK.</p><p>To use JavaScript in a separate application, install the built package with <code>npm install /path/to/kunoworld/sdk/js</code>. Set the same gateway environment variables in that application and place the trusted manifest beside its example file.</p></section><section id="creation-modes" className="docs-section"><h2>Choose your starting point</h2><div className="docs-card-grid">{[['Text to video','Describe the scene, action, camera, and lighting. Available across the standard H3 and LTX profiles.'],['Image to video','Supply one first_frame image to establish the initial composition. Available in the studio.'],['First & last frames','Supply first_frame and last_frame images to define the transition. Supported by H3, LTX Fast, and LTX Pro.'],['Advanced modes','Director supports references, editing, extension, and audio-driven video. LTX adds keyframes and retakes, and Pro adds audio-driven video. All are available in the studio and the SDK.']].map(([title,copy])=><div className="docs-card" key={title}><h3>{title}</h3><p>{copy}</p></div>)}</div><p>Reference images can be PNG, JPEG, or WebP. The gateway limits each encrypted upload to 512 MB by default, and each model sets its own input counts and durations. For advanced modes, inspect <code>limits</code> and <code>modes</code> in the model profile rather than assuming the same inputs work everywhere.</p><CodeBlock code={'const job = await kuno.submit({\n  prompt: "A blown-glass flower turns slowly under soft studio light.",\n  model: "ltx-2.5-fast",\n  mode: "image_to_video",\n  inputs: [{ role: "first_frame", file: imageFile }],\n  durationS: 5,\n  resolution: "1080p",\n});'}/></section><section id="model-routing" className="docs-section"><h2>Understand model routing</h2><p>Before submitting a job, the SDK requests a route for the selected mode and model. The gateway considers enabled profiles, licensing-region policy, and available workers. It may return a compatible fallback with a reason such as <code>region</code>, <code>switched_off</code>, or <code>capacity</code>.</p><p>When a fallback is selected, the SDK adapts duration, resolution, aspect ratio, and frame rate to the fallback profile. The returned job handle includes <code>profileId</code> and <code>fallbackReason</code>. Preserve those values in your application so creators know which profile is actually used.</p><p>Prices shown on the website are placeholder prices from the current profile definitions; final prices will be set before launch. Each profile has a Private price and, where it offers Standard, a lower Standard price. Full MiniMax H3 and H3 Director are offered in Private mode only, and every job costs at least $0.10. <code>GET /v1/models</code> may include <code>pricing_placeholder: true</code> while that is the case. The gateway holds the price when a job is submitted and refunds it automatically if the job fails, is blocked, is canceled, or times out.</p><a href="/models">Compare the model profiles <ArrowUpRight size={13} className="inline"/></a></section><section id="receipts" className="docs-section"><h2>Keep the result and its receipt</h2><p><code>kuno.wait(job)</code> polls the job until it succeeds, fails, or is canceled. On success the SDK checks the ciphertext digest and receipt signature, decrypts the video, and checks its content digest. The result contains the video bytes, receipt, job ID, selected profile, and fallback reason.</p><CodeBlock code={'const result = await kuno.wait(job, {\n  onProgress: (status) => console.log(status.status),\n  timeoutMs: 30 * 60 * 1000,\n});\n\n// Node.js example: keep both files.\nawait fs.writeFile("world.mp4", result.video);\nawait fs.writeFile(\n  "world.receipt.json",\n  JSON.stringify(result.receipt, null, 2),\n);'}/><p>In the Node example, import <code>fs</code> from <code>node:fs/promises</code>. In the browser, use Blob download links as the studio does.</p><div className="notice">A job handle contains the output decryption key. Treat it like a password. If your application persists handles, design secure storage and backups explicitly: KunoWorld can’t recover a lost key, and without it a private video can’t be opened. The studio saves handles in this browser’s local storage and lets you back them up, restore them, or forget them; with <a href="#keys">key sync</a> it also keeps them with your account, encrypted so that KunoWorld can’t read them. Stored videos stay on Cloudflare R2 in both modes until their owner deletes them; nothing expires automatically.</div><p>To delete a video, call <code>delete</code> in either SDK, or <code>DELETE /v1/videos/{'{job_id}'}</code>. It works for the owner in either mode. For a private job it deletes the stored encrypted files; for a standard job, the video, prompt, inputs, and preview. Billing records, job metadata, and the signed receipt stay.</p><CodeBlock code={'// JavaScript\nawait kuno.delete(jobId);\n\n# Python\nkuno.delete(job_id)'}/></section><section id="errors" className="docs-section"><h2>When something needs attention</h2><h3>The studio asks you to sign in</h3><p>Your session has ended or the sign-in link expired. <a href="/signin">Sign in</a> again with your email. The studio never needs a gateway URL or an API key.</p><h3>Cannot connect to the gateway (SDK)</h3><p>This applies to your own programs using the SDKs or the HTTP API. Check the base URL, gateway availability, and API key. An HTTPS page cannot generally connect to an unencrypted remote HTTP service. Keep API keys in server-side code rather than in a browser.</p><h3>No worker with valid attestation</h3><p>The gateway may have no fresh capacity, or worker evidence may not match your pinned manifest. Check the worker profile and evidence age. Do not bypass evidence verification to force a result through.</p><h3>A take was blocked</h3><p><code>content_policy</code> or <code>safety_blocked</code> means the request broke the content policy, which bans all sexual or explicit content. Blocked takes count as strikes against the account.</p><h3>Unsupported parameters</h3><p>A 422 response means the request failed validation. Check the model’s current <code>limits.sizes</code>, duration range, supported modes, and input roles. LTX 4K, for example, does not expose the same modes and formats as LTX Fast. See the <a href="/api#errors">API error reference</a> for response formats and other status codes.</p><h3>A generation takes longer than expected</h3><p>Keep the job handle. Your application can call <code>kuno.status(job.jobId)</code> or continue waiting. Calling <code>kuno.cancel(job.jobId)</code> requests cancellation. Stopping client-side polling alone does not cancel a server job.</p><h3>Receipt or digest verification fails</h3><p>Do not treat the file as a verified result. The SDK rejects mismatched ciphertext, signatures, job IDs, decryption keys, or plaintext digests. Preserve non-sensitive error details for investigation.</p></section><section id="project-status" className="docs-section"><h2>What is ready today</h2><p>The current source implements email sign-in, the studio’s same-origin connection through the website’s server, browser and SDK encryption, Private and Standard modes, model routing, ciphertext uploads, job handling, deletion, local decryption, signed receipt verification, and a mock worker loop. These flows can be tested end to end.</p><h3>Still in development</h3><ul><li>Real GPU serving. The H3 and LTX-2.5 backends and their resident runtimes are written but have not yet run on GPUs.</li><li>Production Intel TDX and NVIDIA evidence verification.</li><li>The confidential VM image and production manifest publication.</li><li>Live validation. Accounts, payments, webhooks, and production storage are built but have not been validated in production.</li><li>Final prices. Current prices are placeholders.</li></ul><p>Until production hardware verification and deployment are complete, this project does not provide the intended hardware privacy guarantees. Read the <a href="/privacy">privacy and provenance guide</a> for the complete boundaries.</p></section></article></div>}
