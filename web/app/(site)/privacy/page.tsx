import type { Metadata } from "next";
import { LockKeyhole, Fingerprint, Cpu, ArrowUpRight } from "lucide-react";
export const metadata:Metadata={title:"Privacy & provenance — KunoWorld",description:"Understand KunoWorld’s encrypted video protocol, signed receipts, current development status, and privacy boundaries."};
export default function Privacy(){return <><header className="page-heading"><span className="section-kicker">YOUR WORLD. YOUR CREATION.</span><h1>Free to imagine.<br/><em>Clear about trust.</em></h1><p>Privacy is a design choice. Verifiability is a way to check it. Here’s what KunoWorld is building—and where the boundaries are today.</p></header><div className="inner-content"><div className="notice"><strong>Prices are placeholders.</strong> Every price shown on KunoWorld is a placeholder until launch pricing is set.</div><div className="notice"><strong>Current state: development preview.</strong> The encryption and receipt protocol is tested with simulated workers. Real Intel TDX and NVIDIA evidence verification, confidential VM deployment, and production GPU serving are unfinished. This code does not yet provide production hardware privacy guarantees.</div><div className="resource-grid">{[{icon:LockKeyhole,title:'Encrypted on your device',copy:'In Private mode, the studio or SDK seals your prompt and reference files to a worker’s key before they leave your device. This website’s server and the gateway pass along ciphertext with the public metadata needed to route the job.'},{icon:Cpu,title:'An intended trusted runtime',copy:'The protocol binds worker keys and model profiles to attestation evidence. Real hardware verification is not complete; current development networks accept simulated evidence.'},{icon:Fingerprint,title:'A receipt you can keep',copy:'The worker signs digests and generation metadata. Your SDK checks the signature, ciphertext, and decrypted video digest before accepting the result.'}].map(c=><div className="resource-card" key={c.title}><c.icon size={28} strokeWidth={1.3}/><h2>{c.title}</h2><p>{c.copy}</p></div>)}</div><div className="about-copy"><h2>Privacy has<br/><em>real boundaries.</em></h2><div><p>In Private mode (the default), KunoWorld sees metadata only: your account, the model, duration, resolution, aspect ratio, frame rate, input roles, file sizes, timing, price, status, and the receipt. It can’t see your prompt, inputs, or video. Encryption does not conceal that metadata.</p><p>In Standard mode, KunoWorld’s systems and the GPU provider, which may not use a confidential GPU, can read your prompt, inputs, and video. In the product, only your account can open it.</p><p>The key that opens a private video stays on your own devices. A compromised device can expose its contents. The intended confidential-computing model also excludes physical attacks on worker hardware.</p><p>A signed receipt establishes a relationship between a job, its output, and a signing key. It does not prove that the generated scene happened in the real world or that the model is always well-behaved.</p></div></div><div className="about-copy"><h2>Your session.<br/><em>Your files.</em></h2><div><p>You sign in with a one-time email link. This website’s server keeps your session in an HttpOnly cookie that page scripts can’t read, and the studio reaches KunoWorld through that server on the same site. No API key or token is ever placed in the page. For a private take, encryption happens in your browser first, so the website’s server only passes ciphertext through. API keys exist only for developers’ own programs.</p><p>Videos in both modes are stored on Cloudflare R2 until you delete them. Nothing expires automatically. <strong>Delete</strong> removes the stored encrypted files of a private take, or the video, prompt, inputs, and preview of a standard take. Billing records, job metadata, and the signed receipt stay. Content under a legal hold or a child sexual abuse material report is kept until the hold ends.</p><p>The studio saves each private take’s key in this browser’s local storage, with its prompt and settings, so your library survives a reload. Anyone who can use this browser profile can open those films. Use <strong>Back up film keys</strong> to keep a copy somewhere safe: KunoWorld can’t recover a lost key, and without it the video is lost. <strong>Forget all</strong> removes the keys from this browser.</p><p>KunoWorld moderators and administrators sign in by email and are given their role. They can open content only for a report of child sexual abuse material or sexual content involving a minor, or under a legal hold, and every view is logged. There is no sampled review. To open a private video they also need its output key, supplied in such a report; reports accept output keys for those two reasons only.</p><p>All sexual or explicit content is banned in both modes. A standard request that breaks the policy is refused, a private take blocked inside the confidential GPU fails, and blocked takes count as strikes on the account. Payments are built but not yet validated live.</p><p>The showcase imagery and videos on this site were generated through external models using OpenRouter. They are separate from the KunoWorld encrypted generation flow and do not demonstrate hardware confidentiality.</p></div></div><a href="/docs#receipts" className="ocean-button button-dark">Learn about result verification <ArrowUpRight size={16}/></a></div></>}
