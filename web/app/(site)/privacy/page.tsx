import type { Metadata } from "next";
import { LockKeyhole, Fingerprint, Cpu, ArrowUpRight } from "lucide-react";
export const metadata:Metadata={title:"Privacy & provenance — KunoWorld",description:"Understand KunoWorld’s encrypted video protocol, signed receipts, current development status, and privacy boundaries."};
export default function Privacy(){return <><header className="page-heading"><span className="section-kicker">YOUR WORLD. YOUR CREATION.</span><h1>Free to imagine.<br/><em>Clear about trust.</em></h1><p>Privacy is a design choice. Verifiability is a way to check it. Here’s what KunoWorld is building—and where the boundaries are today.</p></header><div className="inner-content"><div className="notice"><strong>Current state: development preview.</strong> The encryption and receipt protocol is tested with simulated workers. Real Intel TDX and NVIDIA evidence verification, confidential VM deployment, and production GPU serving are unfinished. This code does not yet provide production hardware privacy guarantees.</div><div className="resource-grid">{[{icon:LockKeyhole,title:'Encrypted on your device',copy:'The SDK seals your prompt and reference files to a worker’s key before uploading them. The gateway handles ciphertext alongside the public metadata required to route the job.'},{icon:Cpu,title:'An intended trusted runtime',copy:'The protocol binds worker keys and model profiles to attestation evidence. Real hardware verification is not complete; current development networks accept simulated evidence.'},{icon:Fingerprint,title:'A receipt you can keep',copy:'The worker signs digests and generation metadata. Your SDK checks the signature, ciphertext, and decrypted video digest before accepting the result.'}].map(c=><div className="resource-card" key={c.title}><c.icon size={28} strokeWidth={1.3}/><h2>{c.title}</h2><p>{c.copy}</p></div>)}</div><div className="about-copy"><h2>Privacy has<br/><em>real boundaries.</em></h2><div><p>The gateway sees account, model, duration, resolution, and input roles. Traffic timing and ciphertext sizes can also be observed. Encryption does not conceal every piece of metadata.</p><p>Your own device holds the decryption key. A compromised device can expose its contents. The intended confidential-computing model also excludes physical attacks on worker hardware.</p><p>A signed receipt establishes a relationship between a job, its output, and a signing key. It does not prove that the generated scene happened in the real world or that the model is always well-behaved.</p></div></div><div className="about-copy"><h2>Your session.<br/><em>Your files.</em></h2><div><p>The studio keeps your API key in memory for the session. It saves each take in this browser’s local storage — the prompt, its settings, and the handle holding the key that decrypts the film — so your library survives a reload. Decrypted videos stay in memory and are fetched and decrypted again when you reopen a take. Anyone who can use this browser profile can open those films; <strong>Forget all</strong> removes them.</p><p>When connected, the studio sends encrypted requests directly to the gateway you configure. The gateway’s retention and account handling are controlled by its operator. You sign in with a one-time email link, and this website’s server keeps your session in an HttpOnly cookie that page scripts can’t read; the studio only receives a token that expires within the hour. Payments are not live yet.</p><p>The showcase imagery and videos on this site were generated through external models using OpenRouter. They are separate from the KunoWorld encrypted generation flow and do not demonstrate hardware confidentiality.</p></div></div><a href="/docs#receipts" className="ocean-button button-dark">Learn about result verification <ArrowUpRight size={16}/></a></div></>}
