import type { Metadata } from "next";
import { ArrowUpRight } from "lucide-react";
import { CodeBlock } from "@/components/site/code-block";
export const metadata:Metadata={title:"API reference — KunoWorld",description:"KunoWorld gateway v1 reference: model discovery, routing, encrypted blobs, video jobs, cancellation, deletion, and provenance."};
const endpoints=[
 {id:'models',method:'GET',path:'/v1/models',auth:false,description:'List model profiles with their capabilities, pricing, region availability, enablement, and worker counts. Prices are placeholders until they are set before launch. Use this response to populate generation controls.',code:'curl "$KUNO_GATEWAY_URL/v1/models"',fields:[['models','Model profiles, each including limits, modes, pricing, enabled, available_in_region, and workers.'],['pricing_placeholder','May be present and true while prices are placeholders. Holds and refunds work the same way.'],['country','Country determined by the gateway for routing policy.'],['workers_online','Distinct fresh workers across the network.'],['switch','Current model-family switch configuration.']]},
 {id:'manifest',method:'GET',path:'/v1/manifest',auth:false,description:'Retrieve the gateway’s golden manifest. A trusted application should pin an independently verified manifest rather than blindly treating a gateway response as a new trust root.',code:'curl "$KUNO_GATEWAY_URL/v1/manifest"',fields:[['allowed','Approved image measurements and supported profile bindings.'],['mock_quote_keys','Development quote-signing keys. Production manifests must not trust mock keys.'],['max_evidence_age_s','Maximum age of worker evidence.']]},
 {id:'switch',method:'GET',path:'/v1/switch',auth:false,description:'Read the owner-signed model switch. It controls enabled model families and the routing configuration shared by the gateway and validators.',code:'curl "$KUNO_GATEWAY_URL/v1/switch"',fields:[]},
 {id:'route',method:'GET',path:'/v1/route',auth:false,description:'Resolve a compatible model profile and obtain candidate workers. No credential is needed; with an API key, the account’s standing is checked as well. The SDK verifies candidate evidence and key bindings against its trusted manifest before encrypting the job.',code:'curl --get "$KUNO_GATEWAY_URL/v1/route" \\\n  --data-urlencode "mode=text_to_video" \\\n  --data-urlencode "profile_id=ltx-2.5-fast"',fields:[['mode','Required creation mode, such as text_to_video or image_to_video.'],['profile_id','Optional preferred model profile.'],['family','Optional preferred model family.'],['privacy','private (default) or standard. Only enclaves whose tier serves that mode are returned.'],['resolution, aspect_ratio, fps, duration_s','Optional. Only workers whose hardware can fit such a video are returned; an omitted field matches any value. When workers serve the model but none can fit it, the answer is 503 no_capacity with max_duration_s, the longest available at that size and frame rate. The SDKs send them.'],['fallback_reason','Response reason: region, switched_off, capacity, or null.'],['enclaves','Candidate workers and their evidence, public keys, capacity, and envelope: the longest duration each serves by resolution, aspect ratio and fps, or null for the model’s full limits.']]},
 {id:'quote',method:'POST',path:'/v1/quote',auth:false,description:'The exact price the gateway would hold for a job if it were submitted now, before anything is encrypted or uploaded. Send only the job’s shape, never a prompt or input. It is routed as /v1/route routes it (switch, licence region, capacity, serving envelopes), the SDKs’ defaults are filled in, and the params are priced with the function that charges the job. With an API key the account’s standing is checked and its balance returned. Limited per account, or per network without a key (429 rate_limited).',code:'curl "$KUNO_GATEWAY_URL/v1/quote" \\\n  -H "Authorization: Bearer $KUNO_API_KEY" -H "content-type: application/json" \\\n  -d \'{"profile_id":"ltx-2.5-fast","resolution":"720p","shots":[{"duration_s":5},{"duration_s":5},{"duration_s":5,"join":"cut"}]}\'',fields:[['profile_id, family, mode','Optional, as for /v1/route. Without mode: storyboard with shots, else inferred from input_roles.'],['privacy','private (default) or standard.'],['duration_s, resolution, aspect_ratio, fps, audio','Optional; the SDKs’ defaults fill the rest.'],['input_roles','The roles of the inputs the job will send; default, what the mode requires.'],['shots','A storyboard: [{duration_s, join}] only, never prompts.'],['mode plan','A plan job: duration_s is the target (required) and no inputs are taken. The price is flat, and breakdown adds plan_usd, with usd_per_second null and billable_seconds 0.'],['price_usd','The hold, in USD.'],['profile_id, fallback_reason, params','The model that would serve it, why if it differs, and the exact params priced.'],['breakdown','usd_per_second × billable_seconds (stitched seconds for a storyboard) × fps_multiplier × long_clip_multiplier = subtotal_usd; min_job_usd and minimum_applied.'],['placeholder','True while prices are placeholders.'],['balance_usd, balance_covers','With a working key; otherwise null.'],['errors','The job’s own codes: invalid_params, invalid_shots, privacy_mode_unavailable, region_restricted, model_disabled, no_capacity (with max_duration_s), private_mode_not_eligible, rate_limited.']]},
 {id:'upload',method:'POST',path:'/v1/blobs',auth:true,description:'Upload encrypted reference media as application/octet-stream. The body must use the KunoWorld KUNOB1 blob format. Plaintext uploads are rejected. Use the SDK to encrypt and upload media.',code:'// SDK performs the encrypted upload for each input.\nconst job = await kuno.submit({\n  prompt: "Paper lanterns sway gently between the bookshelves.",\n  model: "ltx-2.5-fast",\n  mode: "image_to_video",\n  inputs: [{ role: "first_frame", file: imageFile }],\n});',fields:[['blob_id','Identifier for the stored ciphertext.'],['sha256','Digest of the stored ciphertext.'],['size','Stored ciphertext size in bytes.']]},
 {id:'download',method:'GET',path:'/v1/blobs/{blob_id}',auth:true,description:'Download an account-accessible encrypted blob as application/octet-stream. A missing or inaccessible blob returns 404; a blob that was deleted or removed returns 410. Downloading ciphertext does not decrypt or verify it.',code:'// Download, verify, and decrypt through the SDK.\nconst result = await kuno.result(job);',fields:[]},
 {id:'submit',method:'POST',path:'/v1/videos',auth:true,description:'Submit a sealed job envelope. Public generation parameters are bound as associated data to the encrypted payload. Never send plaintext prompt fields directly to this endpoint; use the SDK. This endpoint creates private jobs only: an account that can’t make private jobs gets 403 private_mode_not_eligible with reasons, and an enclave outside the confidential tier is refused with 409 enclave_unavailable. A take blocked inside the enclave by the content policy fails with safety_blocked.',code:'const job = await kuno.submit({\n  prompt: "A copper sculpture floats in warm studio light.",\n  model: "ltx-2.5-fast",\n  durationS: 5,\n  resolution: "1080p",\n  aspectRatio: "16:9",\n  audio: true,\n});',fields:[['job_id','Client-generated UUID for this generation.'],['params','Public settings: profile, mode, duration, resolution, aspect ratio, fps, audio, and input roles. A storyboard adds shots: [{duration_s, join}], and its duration_s is the stitched length.'],['enclave_id','Selected worker identity.'],['enc','Base64url HPKE encapsulated key.'],['ciphertext','Sealed private payload containing the prompt and private options; for a storyboard, the scene and one prompt per shot.'],['input_blob_ids','Encrypted input blobs, bound to the job.']]},
 {id:'list',method:'GET',path:'/v1/videos',auth:true,description:'List jobs owned by the authenticated account. The default limit is 50. Returned job metadata does not restore decryption keys lost by your application.',code:'const jobs = await kuno.list(50);',fields:[['limit','Maximum number of jobs to return, constrained by the gateway.']]},
 {id:'status',method:'GET',path:'/v1/videos/{job_id}',auth:true,description:'Read a job’s status and public generation metadata. Completed jobs include an output blob ID and receipt. Polling does not generate an additional video.',code:'const status = await kuno.status(job.jobId);\nconsole.log(status.status, status.progress);',fields:[['status','Lifecycle state of the job.'],['progress','Progress reported by the worker.'],['output_blob_id','Encrypted result blob when available.'],['receipt','Signed receipt when available.'],['error_code / error','Machine-readable and human-readable failure details.'],['privacy','"private" (the default) or "standard", reported for every job.']]},
 {id:'cancel',method:'POST',path:'/v1/videos/{job_id}/cancel',auth:true,description:'Request cancellation of an account-owned job. The gateway returns the resulting job status. Already completed or terminal jobs are returned without restarting work.',code:'const status = await kuno.cancel(job.jobId);',fields:[]},
 {id:'delete',method:'DELETE',path:'/v1/videos/{job_id}',auth:true,description:'Delete a job’s stored content. Owner only, for either mode; returns 204. For a private job it deletes the sealed blobs; for a standard job, the stored video, prompt, inputs, and preview. Nothing is deleted automatically, so videos stay stored until you call this. The charge record, job metadata, and signed receipt stay. Content under a preservation hold (a legal hold or a child sexual abuse material report) is kept until the hold ends. In Python, call kuno.delete(job_id).',code:'await kuno.delete(jobId);',fields:[]},
 {id:'provenance',method:'GET',path:'/v1/provenance/{content_digest}',auth:false,description:'Look up provenance by the SHA-256 digest of the decrypted video file. Returns the receipt, gateway signature check, model attribution, and worker evidence when a matching result exists. Independently verify evidence if your application needs a stronger trust boundary.',code:'const provenance = await kuno.provenance(videoFile);\n// Or, if you already have its SHA-256 digest:\nconst same = await kuno.provenanceByDigest(digest);',fields:[['receipt','Signed generation receipt.'],['signature_valid','Signature check performed by the gateway.'],['model','Profile ID, name, and attribution.'],['enclave','Worker identity, hardware metadata, and evidence.']]},
 {id:'plans-create',method:'POST',path:'/v1/plans',auth:true,description:'Submit a sealed plan job: a storyboard written from your brief inside the enclave, with nothing rendered. The same envelope as POST /v1/videos with params.mode "plan" (POST /v1/videos accepts it too). Plans run only on confidential workers whose /v1/route entries list the plan/1 feature; any other worker is refused with 409 enclave_unavailable. The price is flat, $0.10, whatever the target length. The gateway sees the frame and target length, the price, the status and the receipt; never the brief, the style or the plan. The output blob is the sealed plan JSON, and the receipt carries plan instead of video. If the planner writes nothing usable the job fails with plan_failed and is refunded. Render the plan as an ordinary storyboard: its scene is the prompt, its shots the shots.',code:'const { plan } = await kuno.plan({\n  brief: "A 30-second ad for a small coffee roastery, warm and handmade.",\n  targetS: 30,\n  style: "35mm film, warm",\n});\nconst job = await kuno.submit({ prompt: plan.scene, shots: planToShots(plan), model: plan.profile_id,\n  resolution: plan.resolution, aspectRatio: plan.aspect_ratio, fps: plan.fps, audio: plan.audio });',fields:[['params','{profile_id, mode: "plan", duration_s (the target, 4 to 120 s), resolution, aspect_ratio, fps, audio, input_roles: []}. No shots.'],['ciphertext','Sealed brief as prompt (at most 4,000 characters) and options.plan: {style?, max_shot_s?, min_shots?, max_shots?, revise?: {plan, instruction, shots?}}. No negative prompt. The SDKs set max_shot_s to the longest shot a routable plan worker’s envelope serves at this size and frame rate.'],['output','Decrypt the output blob with the job’s output key under the label <job_id>/output/plan, refuse anything but the padded framing, and unpad; the JSON’s SHA-256 is the receipt’s content_digest.'],['plan','Plan v1: {v, profile_id, resolution, aspect_ratio, fps, audio, target_s, duration_s (the shots’ exact stitched length), title, scene, shots: [{beat, prompt, duration_s, join}], notes, repairs, planner}.'],['errors','enclave_unavailable, envelope_exceeded, invalid_params, rate_limited (plans per minute), and as failures plan_failed and safety_blocked, both refunded.']]},
 {id:'plans-status',method:'GET',path:'/v1/plans/{job_id}',auth:true,description:'A plan job’s status, in either privacy mode. The same JobStatus as GET /v1/videos/{job_id}; 404 for a job that isn’t a plan. The stage reads planning, then checking.',code:'curl "$KUNO_GATEWAY_URL/v1/plans/$JOB_ID" -H "Authorization: Bearer $KUNO_API_KEY"',fields:[]},
 {id:'standard-upload',method:'POST',path:'/v1/standard/uploads?role={role}',auth:true,description:'Standard mode only. Upload one input as it is, with its input role as role. The gateway detects the file type from its bytes and ignores Content-Type; an unrecognized file is 422 unsupported_media. KunoWorld can read it. Uploads are scanned: a match returns 422 upload_blocked with a generic message, and 503 scan_unavailable means the scanner couldn’t be reached. Uploads that no job uses expire after 24 hours; an upload a job uses stays with that job until the owner deletes it.',code:'curl -X POST "$KUNO_GATEWAY_URL/v1/standard/uploads?role=first_frame" \\\n  -H "Authorization: Bearer $KUNO_API_KEY" \\\n  -H "Content-Type: image/png" \\\n  --data-binary @frame.png',fields:[['upload_id','Pass it in a standard job’s inputs.'],['sha256','Digest of the uploaded file.'],['size','Size in bytes.'],['mime','The file’s MIME type.']]},
 {id:'standard-create',method:'POST',path:'/v1/standard/videos',auth:true,description:'Create a standard job from a readable prompt. KunoWorld’s systems and the GPU provider can read the prompt, inputs and video. The gateway validates the parameters, picks a worker of any tier, sets an explicit seed when none is given, seals the job to that worker, and charges it like a private job. A request that breaks the content policy, which bans all sexual or explicit content, is refused with 422 content_policy and counts as a strike. Returns 201 with the job status, whose privacy is "standard".',code:'const job = await kuno.submit({\n  prompt: "A paper boat crosses a rain puddle.",\n  model: "ltx-2.5-fast",\n  privacy: "standard",\n});\nconst { video } = await kuno.wait(job);',fields:[['job_id','Optional client-chosen UUID.'],['params','The same GenerationParams a private job uses.'],['prompt / negative_prompt / seed / options','The readable request. For a storyboard, prompt is the scene every shot shares and may be empty.'],['shots','Storyboards only: [{prompt}], one per params.shots, in order.'],['inputs','[{upload_id, index, role, time_s?, strength?, hint?, start_s?, end_s?}]'],['webhook_url','Optional https URL for completion notices.']]},
 {id:'standard-list',method:'GET',path:'/v1/standard/videos?limit=50',auth:true,description:'This account’s standard jobs, newest first. Private jobs never appear here. Plans are listed at GET /v1/standard/plans.',code:'const videos = await kuno.listStandard(50);',fields:[['job_id / status / profile_id / params / prompt','The job and its request.'],['created_at / finished_at','Unix seconds.'],['has_video','Whether a stored video can be downloaded.'],['error_code','Failure reason, if any.'],['deleted','True once the owner deleted it or it was removed after a report.']]},
 {id:'standard-video',method:'GET',path:'/v1/standard/videos/{job_id}/video',auth:true,description:'Download a standard job’s video as video/mp4. Owner only. Returns 404 not_ready until the job succeeds, and 410 with code deleted (the owner deleted it) or removed (removed after a report). The thumbnail endpoint answers the same way.',code:'const video = await kuno.standardVideo(jobId);\n// Or check it against the job’s receipt too:\nconst result = await kuno.result(job);',fields:[]},
 {id:'standard-thumbnail',method:'GET',path:'/v1/standard/videos/{job_id}/thumbnail',auth:true,description:'A JPEG frame from a standard job’s video. Owner only.',code:'const jpeg = await kuno.standardThumbnail(jobId);',fields:[]},
 {id:'standard-delete',method:'DELETE',path:'/v1/standard/videos/{job_id}',auth:true,description:'Equivalent to DELETE /v1/videos/{job_id} for a standard job: deletes the stored video, prompt, inputs, and preview. Returns 204. The billing record, job metadata, and receipt stay.',code:'await kuno.deleteStandard(jobId);\n// Same as:\nawait kuno.delete(jobId);',fields:[]},
 {id:'standard-plans-create',method:'POST',path:'/v1/standard/plans',auth:true,description:'Standard mode: KunoWorld and the GPU provider can read the brief and the plan. The brief, style and any revision are checked against the content policy (422 content_policy counts as a strike). The job runs on a confidential worker that writes plans and costs a flat $0.08. The delivered plan is checked again, stored until you delete it, and read back with GET /v1/standard/plans/{job_id}. To render it, send the plan’s storyboard params (mode storyboard, its duration_s and each shot’s duration_s and join), its scene as prompt, and shots: [{prompt}] to POST /v1/standard/videos.',code:'curl "$KUNO_GATEWAY_URL/v1/standard/plans" \\\n  -H "Authorization: Bearer $KUNO_API_KEY" -H "content-type: application/json" \\\n  -d \'{"params":{"profile_id":"ltx-2.5-fast","mode":"plan","duration_s":30,"resolution":"720p","aspect_ratio":"16:9","fps":24},"brief":"A lighthouse keeper lights the lamp as a storm rolls in.","style":"35mm film, warm"}\'',fields:[['params','GenerationParams with mode plan; duration_s is the target length.'],['brief','At most 4,000 characters; empty only in a revision.'],['style','Optional, at most 500 characters (or options.style, not both).'],['options','Optional PlanOptions: max_shot_s, min_shots, max_shots, revise.'],['errors','content_policy, prompt_too_long, invalid_brief, invalid_options, invalid_params, no_capacity, rate_limited.']]},
 {id:'standard-plans-get',method:'GET',path:'/v1/standard/plans/{job_id}',auth:true,description:'The delivered plan as application/json: Plan v1, byte for byte the JSON the receipt’s content_digest covers. Owner only; 404 not_ready until the job succeeds; 410 deleted or removed. GET /v1/standard/plans lists your plans with brief, style and plan; DELETE /v1/standard/plans/{job_id} deletes one.',code:'curl "$KUNO_GATEWAY_URL/v1/standard/plans/$JOB_ID" -H "Authorization: Bearer $KUNO_API_KEY"',fields:[['v, profile_id, resolution, aspect_ratio, fps, audio','The frame of the storyboard.'],['target_s, duration_s','The target, and the exact stitched length of the shots.'],['title, scene, notes','The scene is the storyboard’s prompt.'],['shots','[{beat, prompt, duration_s, join}], 2 to 12.'],['repairs','Every change code made to what the planner wrote.'],['planner','{model, prompt_version}.']]},
 {id:'eligibility',method:'GET',path:'/v1/account/eligibility',auth:true,description:'Whether this account can make private jobs, and any restriction. By default private mode needs a credited top-up or operator credit, no active restriction, and fewer than 2 strikes in 30 days. Every blocked take is a strike: a standard job refused with content_policy, a job that fails with safety_blocked in either mode, and an upload refused with upload_blocked. A job blocked only for text a model wrote inside the enclave (an enhanced prompt or a Director plan) is refused and refunded without a strike. By default 3 strikes in 24 hours restrict the account for an hour, 5 in 7 days for 7 days, and 10 in 30 days until an operator reviews it. The KunoWorld website reads the same information through its own server.',code:'const { private_mode, restricted_until } = await kuno.eligibility();\nif (!private_mode.eligible) console.log(private_mode.reasons);',fields:[['private_mode','{eligible, reasons}'],['restricted_until','Unix seconds, or null when the account isn’t restricted. A restriction that lasts until review is 253402300799 (year 9999).'],['strikes_24h / strikes_7d','Blocked jobs in each window.']]},
 {id:'reports',method:'POST',path:'/v1/reports',auth:false,description:'Report a video. No credential is needed; reports are rate-limited per IP. Identify the video by content_digest, job_id or url. Reports of csam and sexual_minor are prioritised. You may include output_key (base64url) only when reason is csam or sexual_minor, so reviewers can open that one private video; with any other reason the report is refused with 422 key_not_accepted. Returns 202.',code:'await kuno.report({\n  content_digest: sha256,\n  reason: "copyright",\n  details: "This is my film.",\n});',fields:[['reason','csam, sexual_minor, nonconsensual_intimate, violent_extremism, harassment, copyright, or other.'],['details','Optional description.'],['output_key','Optional key of a private video. Accepted only with reason csam or sexual_minor.'],['contact_email','Optional address for follow-up.'],['report_id','The returned reference.']]},
 {id:'shares-create',method:'POST',path:'/v1/videos/{job_id}/shares',auth:true,description:'Create a share link to one of this account’s finished videos. Only the owner can open a video unless they create a link; anyone who has the link can watch that video. Accepts an API key or the website session. Returns 201. The token is shown only in this response: KunoWorld stores only a hash of it, so a lost link can’t be shown again. For a Private video, url has no key, because KunoWorld never has it: the link you share needs the video’s output key after #k=, as in https://kunoworld.com/s/<token>#k=<key>. A video can have up to 20 working links (409 too_many_shares). An account under a restriction gets 403 account_restricted, and a video that hasn’t finished gets 409 not_ready.',code:'curl -X POST "$KUNO_GATEWAY_URL/v1/videos/$JOB_ID/shares" \\\n  -H "Authorization: Bearer $KUNO_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d \'{"expires_at": null}\'',fields:[['expires_at','Optional. Unix seconds, from a minute to ten years ahead; null or absent for a link that works until revoked.'],['share_id','Identifies the link, for listing and revoking.'],['token / url_path / url','The link. Returned only once.'],['job_id / privacy','The video and its mode.'],['status','The link’s status, such as active, revoked or expired.'],['created_at / view_count','When the link was made, and how many times it has been viewed.']]},
 {id:'shares-list',method:'GET',path:'/v1/account/shares?job_id={job_id}',auth:true,description:'This account’s share links, newest first, each with its status and view count. Tokens are never returned again. Accepts an API key or the website session.',code:'curl "$KUNO_GATEWAY_URL/v1/account/shares?job_id=$JOB_ID" \\\n  -H "Authorization: Bearer $KUNO_API_KEY"',fields:[['job_id','Optional. Only the links to this video.'],['share_id / privacy / profile_id / status','Each link, its video’s mode and model, and the link’s status.'],['created_at / expires_at / revoked_at / view_count','Unix seconds or null, and the view count.']]},
 {id:'shares-revoke',method:'DELETE',path:'/v1/account/shares/{share_id}',auth:true,description:'Revoke a share link. It stops working at once, and anyone who opens it sees the same “This link no longer works” message as for every link that has stopped working. Returns the link with its new status. Accepts an API key or the website session.',code:'curl -X DELETE "$KUNO_GATEWAY_URL/v1/account/shares/$SHARE_ID" \\\n  -H "Authorization: Bearer $KUNO_API_KEY"',fields:[]},
 {id:'share-view',method:'GET',path:'/v1/shares/{token}',auth:false,description:'Read a working share link. No credential is needed. Public share requests are rate-limited per IP using a keyed hash of the address, 60 a minute by default (429 rate_limited); IP addresses aren’t stored. Responses are marked noindex and not cached. Once a link stops working (revoked, expired, its video deleted, removed or under a hold, or its account closed) it returns 410 share_unavailable, the same in every case. An unknown token returns 404 not_found.',code:'curl "$KUNO_GATEWAY_URL/v1/shares/$TOKEN"',fields:[['privacy','"private" or "standard".'],['profile_id','The model profile that made the video.'],['created_at / shared_at / expires_at','Unix seconds: when the video was made, when the link was created, and when it expires (null for never).'],['content_digest','SHA-256 of the decrypted video file.'],['receipt','The enclave-signed receipt.'],['signing_public_key','The enclave’s signing public key, for checking the receipt.']]},
 {id:'share-video',method:'GET',path:'/v1/shares/{token}/video',auth:false,description:'Download the shared video. Standard: the video as video/mp4. Private: the sealed ciphertext as application/octet-stream, which only the key after # in the full link opens. Browsers never send that part of a link, so KunoWorld still can’t open the video: decrypt it and check the receipt on the viewer’s side, as the share page does. Each download counts as a view. The rate limit and the 404 and 410 answers are the same as for GET /v1/shares/{token}.',code:'curl -o shared-video "$KUNO_GATEWAY_URL/v1/shares/$TOKEN/video"',fields:[]},
 {id:'me-shares',method:'POST',path:'/v1/me/videos/{job_id}/shares',auth:'session',description:'The website’s share endpoints, which it calls through its own server with the signed-in session: create a link, list links, and revoke a link. Same request and response shapes as the share endpoints above. An API key gets 401.',code:'POST   /v1/me/videos/{job_id}/shares   {"expires_at": null}\nGET    /v1/me/shares\nDELETE /v1/me/shares/{share_id}',fields:[]},
 {id:'keyvault-get',method:'GET',path:'/v1/me/keyvault?cursor=&limit=',auth:'session',description:'Key sync for Private-video keys. The website uses these endpoints with the signed-in session only; an API key gets 401. Returns the vault, its unlockers and a page of wrapped job keys; next_cursor continues. Returns 404 no_vault when key sync is off. Everything in the vault was encrypted in the customer’s browser, and KunoWorld can’t decrypt it.',code:'GET /v1/me/keyvault?cursor=&limit=',fields:[['cursor / limit','Page through the wrapped job keys.']]},
 {id:'keyvault-create',method:'POST',path:'/v1/me/keyvault',auth:'session',description:'Turn key sync on. The browser generates the account master key (32 random bytes), wraps it with each unlocker, and sends only the wrapped copies with their public parameters. The master key never leaves the customer’s devices unwrapped. Returns 201, or 409 vault_exists if key sync is already set up.',code:'POST /v1/me/keyvault\n{"master_key_id": "…", "unlockers": [ … ]}',fields:[['master_key_id','Identifies the master key.'],['unlockers','Each carries the wrapped master key, a label, and public parameters: for a recovery code, its salt and PBKDF2-HMAC-SHA256 iteration count; for a passkey, its credential ID, PRF salt and site ID.']]},
 {id:'keyvault-delete',method:'DELETE',path:'/v1/me/keyvault',auth:'session',description:'Turn key sync off. Deletes the vault, its unlockers and every wrapped job key from KunoWorld. Keys already in a browser stay there. Returns 204.',code:'DELETE /v1/me/keyvault',fields:[]},
 {id:'keyvault-unlocker-add',method:'POST',path:'/v1/me/keyvault/unlockers',auth:'session',description:'Add an unlocker, such as a passkey, that wraps the current master key. Returns 201.',code:'POST /v1/me/keyvault/unlockers\n{"master_key_id": "…", "unlocker": { … }}',fields:[['unlocker','One unlocker, shaped as in POST /v1/me/keyvault.']]},
 {id:'keyvault-unlocker-remove',method:'DELETE',path:'/v1/me/keyvault/unlockers/{unlocker_id}',auth:'session',description:'Remove an unlocker. The last one can’t be removed: 409 last_unlocker. To stop using key sync, turn it off instead.',code:'DELETE /v1/me/keyvault/unlockers/{unlocker_id}',fields:[]},
 {id:'keyvault-job-key-put',method:'PUT',path:'/v1/me/keyvault/job-keys/{job_id}',auth:'session',description:'Store or replace one Private video’s wrapped key record. The browser encrypts the record with AES-256-GCM under the master key, bound to the account and the video. The record holds the output key, the enclave’s signing public key, the content digest, and display details such as the first 500 characters of the prompt and the settings.',code:'PUT /v1/me/keyvault/job-keys/{job_id}\n{"master_key_id": "…", "wrapped": "…"}',fields:[['wrapped','The encrypted key record.']]},
 {id:'keyvault-job-key-delete',method:'DELETE',path:'/v1/me/keyvault/job-keys/{job_id}',auth:'session',description:'Delete one video’s synced key. Deleting the video deletes it too. Returns 204.',code:'DELETE /v1/me/keyvault/job-keys/{job_id}',fields:[]},
 {id:'keyvault-rotate',method:'POST',path:'/v1/me/keyvault/rotate',auth:'session',description:'Replace the master key in one atomic step: new unlockers replace every old one, and every job key is re-wrapped in the browser under the new master key. Old recovery codes and passkeys stop working. expected_version must be the vault’s current version, and job_keys must re-wrap exactly the keys the vault holds; otherwise 409 vault_changed.',code:'POST /v1/me/keyvault/rotate\n{\n  "expected_version": 3,\n  "master_key_id": "…",\n  "unlockers": [ … ],\n  "job_keys": [{ "job_id": "…", "wrapped": "…" }]\n}',fields:[['expected_version','The vault version the rotation starts from.'],['job_keys','[{job_id, wrapped}] for every synced key.']]},
];
export default function APIPage(){return <div className="docs-layout"><aside className="docs-sidebar"><span>GATEWAY API / V1</span><nav aria-label="API reference sections"><a href="#authentication">Authentication</a><a href="#privacy-modes">Privacy modes</a><a href="#share-links">Share links</a><a href="#key-sync">Key sync</a>{endpoints.map(e=><a key={e.id} href={`#${e.id}`}>{({'shares-create':'Share: create a link','shares-list':'Share: list links','shares-revoke':'Share: revoke a link','share-view':'Share: open a link','share-video':'Share: download the video','me-shares':'Website: share links','keyvault-get':'Key sync: read','keyvault-create':'Key sync: turn on','keyvault-delete':'Key sync: turn off','keyvault-unlocker-add':'Key sync: add an unlocker','keyvault-unlocker-remove':'Key sync: remove an unlocker','keyvault-job-key-put':'Key sync: store a key','keyvault-job-key-delete':'Key sync: delete a key','keyvault-rotate':'Key sync: rotate',models:'Model catalog',manifest:'Golden manifest',switch:'Model switch',route:'Routing',upload:'Upload ciphertext',download:'Download ciphertext',submit:'Create a video',list:'List jobs',status:'Job status',cancel:'Cancel a job',delete:'Delete a video',provenance:'Provenance','plans-create':'Create a plan','plans-status':'Plan status','standard-plans-create':'Standard: create a plan','standard-plans-get':'Standard: read a plan','standard-upload':'Standard: upload an input','standard-create':'Standard: create a video','standard-list':'Standard: list videos','standard-video':'Standard: download','standard-thumbnail':'Standard: thumbnail','standard-delete':'Standard: delete',eligibility:'Private-mode eligibility',reports:'Report a video'} as Record<string,string>)[e.id]}</a>)}<a href="#errors">Error responses</a></nav><a href="/docs" className="docs-external">SDK documentation <ArrowUpRight size={13}/></a></aside><article className="docs-main"><span className="section-kicker">API REFERENCE</span><h1>A clear path<br/><em>from idea to video.</em></h1><p className="docs-lead">The KunoWorld gateway API routes encrypted generation requests. Your SDK handles the private payload and result verification.</p><div className="notice">This reference is for developers calling the KunoWorld gateway from their own programs. It is not how the KunoWorld website works: the website uses email sign-in and talks to KunoWorld through its own server. Set <code>KUNO_GATEWAY_URL</code> to your gateway. The showcase asset-generation key is never available through these pages.</div><section id="authentication" className="docs-section"><h2>Authentication</h2><p>The job API — <code>/v1/videos</code>, <code>/v1/blobs</code>, <code>/v1/standard/*</code>, <code>/v1/route</code>, and <code>/v1/account/eligibility</code> — accepts an API key as a bearer token. Create API keys on your <a href="/account">account page</a>. Model discovery, routing, manifest, switch, provenance, and report endpoints are public; routing also checks the account’s standing when a key is sent.</p><p>Share management — <code>POST /v1/videos/{'{job_id}'}/shares</code> and <code>/v1/account/shares</code> — accepts an API key or the website session. The public share endpoints, <code>GET /v1/shares/{'{token}'}</code> and its video, need no credential and are rate-limited per IP. Key sync (<code>/v1/me/keyvault</code>) and the website’s own share endpoints under <code>/v1/me/</code> accept only the website’s signed-in session; an API key gets <code>401</code>.</p><CodeBlock code={'Authorization: Bearer YOUR_KUNO_API_KEY\nContent-Type: application/json'}/><p>API keys belong in server-side code. Never ship one to a browser. The KunoWorld website doesn’t use API keys: it keeps the signed-in session in an HttpOnly cookie and calls KunoWorld from its own server. <code>POST /v1/me/studio-token</code> is retired and returns <code>410 gone</code>, and old <code>kwt_</code> studio tokens are rejected.</p><p>Use the SDK for generation. It binds public parameters to ciphertext and verifies worker evidence before encrypting. A raw prompt is not a valid job envelope.</p></section><section id="privacy-modes" className="docs-section"><h2>Privacy modes</h2><p>Every job is <strong>private</strong> or <strong>standard</strong>. The mode is not part of <code>params</code>, which remain the encryption’s associated data. The gateway records it and reports it as <code>privacy</code> on every job status.</p><ul><li><strong>Private</strong> (the default): encrypted blobs through <code>POST /v1/blobs</code>, then a sealed envelope to <code>POST /v1/videos</code>, encrypted to an attested confidential GPU. The stored video opens only with its key, which stays with you; key sync stores it only encrypted, and a share link carries it only after the <code>#</code>. KunoWorld sees metadata only: account, model, duration, resolution, aspect ratio, frame rate, input roles, sizes, timing, price, status, and the receipt. It needs an eligible account.</li><li><strong>Standard</strong>: readable inputs through <code>POST /v1/standard/uploads</code>, then <code>POST /v1/standard/videos</code>. KunoWorld’s systems and the GPU provider, which may not use a confidential GPU, can read the prompt, inputs and video. It runs on workers of any tier; only the owner’s account can open the video, unless the owner creates a share link for it.</li></ul><p>In both modes the video is stored on Cloudflare R2 until its owner deletes it with <code>DELETE /v1/videos/{'{job_id}'}</code>. Nothing expires automatically. All sexual or explicit content is banned in both modes. Operators can open content only for a report of child sexual abuse material or sexual content involving a minor, or under a legal hold, and every view is logged; for a private video they also need the output key supplied in such a report.</p><p>A restricted account gets <code>403 account_restricted</code>, with <code>restricted_until</code>, in both modes.</p></section><section id="share-links" className="docs-section"><h2>Share links</h2><p>Only the owner can open a video, unless the owner creates a share link for it. Links are off by default. A link opens one finished video, can expire (no expiry, or a chosen time) and can be revoked. A video can have up to 20 working links. Anyone who has the link can watch that video.</p><ul><li><strong>Standard</strong>: KunoWorld serves the video to whoever has the link.</li><li><strong>Private</strong>: the link carries the video’s output key after the <code>#</code>, as in <code>{'https://kunoworld.com/s/<token>#k=<key>'}</code>. Browsers never send that part to a server, so KunoWorld still can’t see the video. The viewer downloads the sealed ciphertext, decrypts it locally and checks the enclave-signed receipt. Anyone with the full link can watch, so treat it like the video itself.</li></ul><p>A link stops working when it is revoked, when it expires, when the video is deleted, when it is removed after a review under the content policy or placed under a legal preservation hold, or when the account is closed; every case returns the same <code>410 share_unavailable</code>. The token is 32 random bytes and KunoWorld stores only a hash of it, so a lost link can’t be shown again. Public share responses are marked noindex and not cached. KunoWorld counts views but doesn’t record who viewed.</p><p>Both SDKs expose these endpoints on the client: <code>shares.create</code>, <code>shares.list</code>, <code>shares.revoke</code>, <code>shares.get</code> and <code>shares.open</code>, in JavaScript and Python.</p></section><section id="key-sync" className="docs-section"><h2>Key sync</h2><p>Key sync keeps each Private video’s key record with the account, encrypted in the browser, so the customer can open Private videos on other devices. It is optional and works only through the website’s signed-in session, never with an API key. The browser generates a 32-byte account master key, encrypts each key record with AES-256-GCM under it, bound to the account and the video, and wraps the master key with one or more unlockers: a recovery code stretched with PBKDF2-HMAC-SHA256 at 600,000 iterations, or a passkey using the WebAuthn PRF extension. KunoWorld stores only the wrapped copies and public unlocker parameters, and can’t decrypt them or recover a lost recovery code.</p></section>{endpoints.map(e=><section id={e.id} className="api-endpoint" key={e.id}><div className="endpoint-title"><span className={`http-method ${e.method==='POST'?'method-post':''}`}>{e.method}</span><code>{e.path}</code><span className="endpoint-auth">{e.auth==='session'?'WEBSITE SESSION ONLY':e.auth?'API KEY REQUIRED':'PUBLIC'}</span></div><p>{e.description}</p><CodeBlock code={e.code}/>{e.fields.length>0&&<dl className="api-field-list">{e.fields.map(([name,description])=><div className="contents" key={name}><dt>{name}</dt><dd>{description}</dd></div>)}</dl>}</section>)}<section className="docs-section" id="errors"><h2>Error responses</h2><p>Gateway application errors return an HTTP status and a <code>detail</code> object with <code>code</code> and <code>message</code>, as shown below. Request-schema and query validation errors return HTTP 422 with a <code>detail</code> array instead.</p><CodeBlock code={'{\n  "detail": {\n    "code": "not_found",\n    "message": "No such blob."\n  }\n}'}/><p>A missing required query parameter, such as <code>mode</code> on <code>/v1/route</code>, produces a validation response like this:</p><CodeBlock code={'{\n  "detail": [\n    {\n      "type": "missing",\n      "loc": ["query", "mode"],\n      "msg": "Field required",\n      "input": null\n    }\n  ]\n}'}/><p>The SDK raises <code>KunoError</code> with <code>status</code>, <code>code</code>, and <code>message</code>, plus the rest of the error body as <code>details</code> (for example <code>reasons</code> or <code>restricted_until</code>). Its current JavaScript client does not preserve validation-array details: those responses use the generic code <code>error</code> and the HTTP status text. Check the HTTP status and request fields when a more specific message is unavailable.</p><dl className="api-field-list">{[['400','The uploaded body is missing the encrypted blob format. Encrypt reference media with the SDK.'],['401','The bearer API key is missing or unknown, or an old kwt_ studio token was sent. Use an API key from your account page. Key sync (/v1/me/keyvault) and the website’s /v1/me/ share endpoints also return 401 to an API key: they accept only the website session.'],['402','The account has insufficient gateway balance for the requested generation.'],['403','private_mode_not_eligible: the account can’t make private jobs yet; the detail lists reasons. account_restricted: too many blocked jobs, so no jobs in either mode until restricted_until. content_not_reviewable (operator API): the item is not a report of child sexual abuse material or sexual content involving a minor, and no matching legal hold covers it, so its content can’t be opened.'],['404','The requested model, account-owned job, blob, or provenance record was not found. not_found also answers an unknown share token or share link. no_vault: key sync is off for this account.'],['409','Routing or submission conflicts: a mode is unavailable, a model is disabled, a worker is unavailable, or the job ID already exists. envelope_exceeded: the chosen worker’s hardware can’t fit the video’s size, frame rate and duration (max_duration_s says how long it can go); route again with resolution, aspect_ratio, fps and duration_s. Inspect the error code before retrying. For share links: too_many_shares (the video already has 20 working links) and not_ready (the video hasn’t finished). For key sync: last_unlocker (the only unlocker can’t be removed) and vault_changed (the vault changed on another device; load it again, then rotate).'],['410','deleted or removed: the blob or standard video was deleted by its owner or removed after a report. gone: a retired endpoint, such as POST /v1/me/studio-token. share_unavailable: a share link no longer works because it was revoked or expired, its video was deleted, removed or placed under a hold, or its account was closed; the answer is the same in every case.'],['413','The encrypted upload exceeds the gateway’s size limit.'],['422','content_policy: a standard request breaks the content policy, which bans all sexual or explicit content. upload_blocked: a standard upload matched the content scan. key_not_accepted: a report included output_key with a reason other than csam or sexual_minor. Also returned for invalid generation parameters, input bindings, encrypted envelope, request schema, or query fields, and for an unsupported model mode.'],['429','rate_limited: too many requests to the public share endpoints from one network. Try again in a minute.'],['451','The requested profile is restricted in the gateway’s region. Routing returns this when no allowed fallback can serve the mode; direct submission rejects the restricted profile.'],['503','scan_unavailable: a standard upload couldn’t be scanned; retry later. no_capacity, or otherwise: no suitable worker capacity is available. Retry routing after a delay; never bypass evidence verification.']].map(([status,description])=><div className="contents" key={status}><dt>{status}</dt><dd>{description}</dd></div>)}</dl><p>A plan job that fails with <code>plan_failed</code> got nothing usable from the planner; it is refunded, so rephrase the brief and try again. A job that fails with <code>safety_blocked</code> was blocked by the content policy inside the enclave; like <code>content_policy</code>, it counts as a strike. If a submission may already have been accepted, check its state using the job ID when you have retained it before submitting again. The JavaScript SDK returns the job handle only after submission succeeds; a failed submission response may leave you without that handle or its decryption key. Listing jobs can help identify accepted work but cannot recover lost keys.</p></section></article></div>}
