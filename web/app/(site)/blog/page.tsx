import type { Metadata } from "next";
import { ArrowUpRight } from "lucide-react";
import { posts } from "@/lib/site-content";
export const metadata:Metadata={title:"Journal — KunoWorld",description:"Creative notes, practical camera guides, and ideas behind private, verifiable AI video."};
export default function Blog(){const first=posts[0];return <><header className="page-heading blog-heading"><span className="section-kicker">THE KUNOWORLD JOURNAL</span><h1>Field notes from<br/><em>the imagination.</em></h1><p>Ideas for your next scene. A little perspective on the tools. And the thinking behind the worlds we make.</p></header><div className="inner-content"><article className="blog-feature"><a href={`/blog/${first.slug}`} className="blog-feature-image"><img src={first.image} alt="Paper lanterns illuminating a monumental spiral library"/></a><div><span className="section-kicker">{first.category} / {first.read}</span><h2>{first.title}</h2><p>{first.description}</p><a className="text-link" href={`/blog/${first.slug}`}>Read the story <ArrowUpRight size={16}/></a></div></article><div className="journal-grid">{posts.slice(1).map(p=><a className="journal-card" href={`/blog/${p.slug}`} key={p.slug}><div className="journal-image"><img src={p.image} alt="" loading="lazy"/><ArrowUpRight size={20}/></div><span>{p.category}<i>•</i>{p.read}</span><h3>{p.title}</h3></a>)}</div></div></>}
